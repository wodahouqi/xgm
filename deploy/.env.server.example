########################################
# 远端后端 .env 模板（将被部署到 REMOTE_ROOT/api/.env）
########################################

# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=change-me
DB_DATABASE=artwork_booking

# JWT 配置
JWT_SECRET=please-change-in-production
JWT_EXPIRES_IN=7d

# 服务器配置
PORT=3000
NODE_ENV=production

# 前端 URL（生产环境用于 CORS）
FRONTEND_URL=https://example.com
UPLOAD_MAX_FILE_SIZE_MB=20
DB_SYNC=true
