SSH_HOST=47.111.190.103
SSH_USER=root
SSH_PORT=22
SSH_PASSWORD=Shcytech@123456

# 站点与域名
DOMAIN=47.111.190.103
NGINX_SITE_NAME=booking-xgm

# 本地目录（前端构建目录与后端源码目录）
LOCAL_FRONTEND_BUILD_DIR=dist
LOCAL_BACKEND_DIR=api

# API 端口（需与 PM2/ecosystem.config.js 默认一致）
API_PORT=3000

# 远端部署根目录
REMOTE_ROOT=/var/www/booking-xgm

# SSL 配置（默认关闭，可按需开启并设置邮箱）
ENABLE_SSL=false
LETSENCRYPT_EMAIL=

# 可选：SSH 主机指纹（避免交互确认），来自首次连接输出
HOSTKEY=SHA256:2KiEkqpYhDoTf1igu1kyJZp6LbCfOubJdyXWf+xTM4Q